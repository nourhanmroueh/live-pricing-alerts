# Python Price Fetcher

This script simulates live price feeds and pushes data to the Laravel backend
via the `/api/prices` endpoint.

## Run
```bash
pip install -r requirements.txt
python fetch_price.py
